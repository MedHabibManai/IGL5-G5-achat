apiVersion: v1
kind: Secret
metadata:
  name: achat-app-secret
  namespace: achat-app
  labels:
    app: achat-app
type: Opaque
stringData:
  # MySQL Database credentials
  # IMPORTANT: Change these values before deploying to production!
  MYSQL_ROOT_PASSWORD: "Admin123456!"
  SPRING_DATASOURCE_USERNAME: "admin"
  SPRING_DATASOURCE_PASSWORD: "Admin123456!"
  
  # Database connection URL will be constructed in deployment
  # Format: jdbc:mysql://<service-name>:3306/achatdb
